name: Deploy
on:
  push:
    branches: [ main, master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Buildah Build
        uses: redhat-actions/buildah-build@v2
        with:
          image: local-alpine
          tags: latest
          containerfiles: |
            ./Dockerfile
      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: local-alpine
          path: /tmp/local-alpine.tar

  push:
    runs-on: ubuntu-latest
    # Podman Login action (https://github.com/redhat-actions/podman-login) also be used to log in,
    # in which case 'username' and 'password' can be omitted.
    needs: build
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v3
        with:
          name: local-alpine
          path: /tmp
      - name: Load image
        run: |
          podman load --input /tmp/local-alpine.tar
          podman image ls -a

      - name: confirm
        run: |
          echo "Docker:";
          docker image ls;
          echo "Podman:";
          podman image ls;
#      - name: Push To quay.io
#        id: push-to-quay
#        uses: redhat-actions/push-to-registry@v2
#        with:
#          image: alpine
#          tags: latest
#          registry: ${{ secrets.QUAY_REGISTRY }} #quay.io/net-sec
#          username: ${{ secrets.QUAY_REGISTRY_USER }} #claudio_walser
#          password: ${{ secrets.QUAY_REGISTRY_PASSWORD }}
